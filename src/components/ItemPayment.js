import React from "react";

export default function ItemPayment(props){

const itemLength =  function(){
    if (i=0,i>16,i++){
        return  <li key={i}> src = 'assets/img/Frame'+ i + '.png'</li>
console(itemLength);
    }
}

    return(
      
<>
</>
    );
}